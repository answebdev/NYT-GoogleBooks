import React from "react";
import { Helmet } from "react-helmet";

const Saved = () => {
    return (
        <div className="container">
            <Helmet>
                <title>NYT Google Books Search | Saved</title>
            </Helmet>
            <br />
            <br />
            <div>
                <p>Saved Page</p>
            </div>
        </div>
    );
};

export default Saved;